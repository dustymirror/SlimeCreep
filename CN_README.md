# Slime Creep v1.13

![版本](https://img.shields.io/badge/version-1.13-blue)
![EuroPi](https://img.shields.io/badge/EuroPi-兼容-green)

**Slime Creep** 是一个为 EuroPi 开发的三通道混沌电压发生器，灵感来自NLC SLOTH。DustyMirror 2026

---

## 功能

- 三个独立的混沌发生器：**Creep**（粘行者）、**Ooze**（渗行者）、**Smear**（抹行者）
- 6 个 CV 输出（每通道 X/Y），0–9.？V 范围
- 每通道独立速度控制（0.3–10 倍率）
- 每通道独立深度控制（0.05–2.0）
- 外部 CV 耦合输入（Ain）
- DIN 触发输入（扰动混沌系统）
- OLED 屏幕实时显示轨迹

---

## 安装

1. 通过 USB 将 EuroPi 连接到电脑
2. 将 `slime_creep.py` 复制到 contrib
3. 增加 ["Slime Creep", "contrib.slime_creep.SlimeCreep"] 到`menu.py`

---

## 硬件接口

| 接口 | 功能 |
|------|------|
| **CV1 / CV4** | 通道 1（Creep）X / Y 输出 |
| **CV2 / CV5** | 通道 2（Ooze）X / Y 输出 |
| **CV3 / CV6** | 通道 3（Smear）X / Y 输出 |
| **K1** | 调节当前选中通道的**速度**，范围 0.3～10.0 |
| **K2** | 调节当前选中通道的**深度**，范围 0.05～2.0 |
| **Ain** | 外部 CV 耦合输入（0～5V），影响所有通道 |
| **B1** | 短按：切换到上一个通道 |
| **B2** | 短按：切换到下一个通道 |
| **B1+B2 同时按** | 回到Europi主菜单 |
| **DIN** | 外部触发输入：扰动所有通道 |

---

## 三通道性格

| 显示名称 | 基频 | 性格描述 |
|----------|------|----------|
|`Creep` | 0.0040 | 最慢，像蜗牛蠕动，适合超长周期调制 |
|`Ooze` | 0.00065 | 中等速度，像黏液渗出，适合包络起伏 |
|`Smear` | 0.000025 | 最快，像涂抹痕迹，适合活跃调制 |

> 三个通道的基础速度不同，但都可以通过 **K1** 独立调节倍率。

---

## OLED 屏幕界面
<img width="399" height="149" alt="截屏2026-05-24 12 56 31" src="https://github.com/user-attachments/assets/367e8cbb-666b-408f-8d3d-80a98f97f14c" />

Ooze ← 当前选中通道      
X10.0 D0.77 D. A= ← 底部状态栏



### 底部状态栏说明

| 字段 | 含义 | 示例 |
|------|------|------|
| `X` | 当前通道的速度值（0.3～10.0） | `X0.3`（最慢）～`X10.0`（最快） |
| `D` | 当前通道的深度值（0.05～2.0） | `D0.05`（浅）～`D2.00`（深） |
| `D.` / `D*` | 扰动指示器：`D*` 表示收到 DIN 触发 | 触发后闪烁约 0.5 秒 |
| `A_` `A-` `A=` `A#` `A@` | Ain 耦合强度（_ 最小，@ 最大） | 外部 CV 越大，符号越密 |

---

## 旋钮控制

### Speed（K1）
- **范围**：0.3～10.0
- **手感**：线性响应，转动直接改变当前通道的速度
- **作用**：乘以该通道的基础速度，数值越大电压变化越快

### Depth（K2）
- **范围**：0.05～2.0
- **手感**：线性响应
- **作用**：控制混沌振子的“力度”，数值越大摆动幅度越强

> 💡 采用**增量控制**：只有转动旋钮时才会改变当前通道的值。切换通道后，新通道会保持之前的参数，不会被旋钮位置覆盖。三个通道可以独立调节，互不干扰。

---

## CV 输出范围

- 每个 CV 输出口输出 **0～9.？V**
- 每通道的 X 和 Y 是独立但相互耦合的混沌信号
- 三通道之间通过 **Ain** 输入耦合

---

## Ain 耦合说明

`Ain` 输入 0～5V 外部 CV 时：
- 该 CV 会影响**所有三个通道**的混沌行为
- 耦合强度在屏幕上用 `A_` → `A-` → `A=` → `A#` → `A@` 表示
- 耦合作用：使三个通道的运动相互拉扯/同步，产生更复杂的集体行为

---

## 扰动说明

当 **DIN** 接收到触发（上升沿）时：
- 所有三个通道同时受到一次随机扰动
- 屏幕上的 `D.` 临时变为 `D*`（持续约 5 帧）
- 扰动幅度与当前选中通道的 `depth` 成正比

用途：为混沌系统注入“意外”，产生不规则的突变。

---

## 按钮操作总结

| 操作 | 结果 |
|------|------|
| 短按 B1 | 切换到上一个通道（Creep ← Ooze ← Smear 循环） |
| 短按 B2 | 切换到下一个通道（Creep → Ooze → Smear 循环） |
| 同时按 B1+B2 | 回到主Europi菜单 |

> 注：长按不放不会触发切换，避免误操作。

---
;@- ;@ ~ .......Enjoy the sludge...... 🐌
